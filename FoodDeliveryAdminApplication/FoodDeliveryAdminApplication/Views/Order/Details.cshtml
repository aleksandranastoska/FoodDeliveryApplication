@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model FoodDeliveryAdminApplication.Models.Order

<style>
    body {
        font-family: 'Roboto', sans-serif;
        background-color: #f8f9fa;
        color: #333;
        margin: 0;
        padding: 0;
    }

    .container {
        max-width: 1200px;
        margin: auto;
        padding: 20px;
    }

    .card {
        border: 1px solid #ddd;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

        .card:hover {
            transform: scale(1.03);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        }

    .card-img-top {
        border-bottom: 1px solid #ddd;
    }

    .card-title {
        font-size: 1.25rem;
        font-weight: bold;
        color: #007bff;
    }

    .card-text {
        font-size: 0.9rem;
        color: #89A8B2;
    }
</style>

<div class="container mt-5">
    <div class="mb-4">
        <h2 class="text-center">Order Details</h2>
        <p class="text-center text-muted">Order for <strong>@Model.Owner.UserName</strong></p>
    </div>

    <div class="row justify-content-center">
        @for (int i = 0; i < Model.FoodsInOrder.Count(); i++)
        {
            var item = Model.FoodsInOrder.ElementAt(i).Food;
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card shadow-sm h-100" style="height: auto;">
                    <img class="card-img-top" src="@item.Image" alt="@item.Name" style="object-fit: cover; height: 200px;">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title text-primary" style="color: #89A8B2 !important">@item.Name</h5>
                        <p class="card-text text-muted">@item.Description</p>
                        <h6 class="mt-auto">Price: <span class="text-success">@item.Price</span></h6>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
